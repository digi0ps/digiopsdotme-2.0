import React from "react";
import ReactDOM from "react-dom";
import App from "./react/app.js";

const root_id = "react-root-container";
const root_element = document.getElementById(root_id);

ReactDOM.render(<App />, root_element);

/*
::FEW JAVASCRIPT CONDUCT RULES FOR WRITING CODE::
- to myself -

1. Use underscore pattern for variable names
2. All file names must start with a lower case
3. Tab Size must be 4?
4. All class names must be lower case and joined by a hyphen
5. Spacing between blocks must be two lines 

*/
